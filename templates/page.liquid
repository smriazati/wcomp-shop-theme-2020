<div class="page-wrapper grid-container grid-container-auto-rows">
  <div class="section-header">
    <h1>{{ page.title }}</h1>
  </div>

  <div class="rte page-content">
    {{ page.content }}
  </div>
</div>
